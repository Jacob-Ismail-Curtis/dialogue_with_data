import { CSSProperties, ReactNode } from 'react';
interface ContextProps {
    addModal: (content: any, options?: {
        className?: string;
        style?: CSSProperties;
        onClose?: any;
        isOpen?: boolean;
    }) => void;
    removeModal: () => void;
}
export declare const ModalProvider: ({ children }: {
    children: ReactNode;
}) => JSX.Element;
export declare const useModal: () => ContextProps;
export {};
