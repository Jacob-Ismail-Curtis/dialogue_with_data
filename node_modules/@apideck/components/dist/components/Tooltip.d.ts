import { CSSProperties, ReactNode } from 'react';
export interface Props {
    text?: string | ReactNode;
    styles?: CSSProperties;
    alignRight?: boolean;
    alignPointerRight?: boolean;
    className?: string;
    children: ReactNode;
}
export declare const Tooltip: ({ children, text, alignRight, alignPointerRight, styles, ...other }: Props) => JSX.Element;
